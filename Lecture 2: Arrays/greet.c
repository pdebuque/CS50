#include <cs50.h>
#include <stdio.h>

int main (int argc, string argv[]) // arguments from the command line. argc: argument count; argv: array of words from the command line
{
  printf("hello, %s\n", argv[1]);
}